Jahshaka patch 0048 — where no probe is, the sky is.

MEDIA-only patch (two Hlms pieces). Studio stages its media from the SOURCE tree
(cmake/IncludeOgre.cmake), so every tree still has to re-run
`irisgl/scripts/build-ogre.sh` to get it — no engine COMPILE is needed beyond the
staging, which is the whole difference from a SOURCE patch. It OVERLAPS patches
0017, 0028 and 0029 in `ForwardPlus_DecalsCubemaps_piece_ps.any` and 0021, 0033
and 0045 in `Vct_piece_ps.any`: a tree that already carries those hits
`build-ogre.sh`'s "Upstream changed the patched file" on a re-run, which is the
documented per-patch reverse-check blind spot and NOT an upstream change. The
cure is the recorded one — reset the submodule
(`git -C irisgl/thirdparty/ogre-next checkout -- . && git ... clean -fd`) and
re-run the script. It travels with ogre-patch 0049 (SOURCE), which the engine
side of the same lane needs.

WHY. The PBS pixel shader has exactly ONE env-probe texture, `texEnvProbeMap`,
and under automatic parallax-corrected cubemaps it holds the probe cube ARRAY.
A datablock that also carries its own manual cubemap generates a shader that
does not compile — three separate ways, listed on `OgreScene::reflectionTexFor`
(OgreSky.cpp) — so the engine takes the sky cubemap OFF every datablock the
moment one probe exists anywhere in the process. That was survivable while a
probe grid was an all-or-nothing scene-wide decision: a grid meant a room, and
in a room the probes ARE the environment.

It stopped being survivable when the grid became a PER PROBE decision (lane
R5-ROOM, patch 0047). A PARTIAL grid is the normal case now — one crate dropped
into a new project keeps 2 of its 18 candidates — and every pixel that no
surviving probe's box contains was left with no environment at all but cone
tracing, whose escape term is a flat, pi-dimmed copy of the same sky and which
outside the voxel volume is nothing. Measured as a bar:
`scripting.e2e.default_ground`'s grazing specular margin fell from 5/255 to
3/255 the day the per-probe rule landed. That margin is this defect.

WHAT THIS PATCH DOES. The sky gets a SECOND slot, at the pass level, through the
extra-pass-texture route HlmsPbs honours for exactly this purpose
(`HlmsListener::getNumExtraPassTextures` / `propertiesMergedPreGenerationStep` /
`hlmsTypeChanged` — upstream's own Terra sample is the reference use, and the
route needs no change to the engine: the slot is reserved inside
`set0_texture_slot_end` and therefore inside the root layout HlmsPbs already
builds). The host half is Jahshaka's `FogHlmsListener` (OgreFog.cpp) and the
declaration lives in Jahshaka's own media (`JahFog_piece_vs_piece_ps.any`'s
`custom_ps_uniformDeclaration`); nothing of either is upstream's. This patch is
the COMPOSITE, which has to live inside upstream's probe loop because nothing
else can see whether a probe answered, plus the two lines in `Vct_piece_ps.any`
that export what the cone could not answer for.

THE TEST IS THE LOOP'S OWN, AND IT IS BINARY: did any probe's box contain this
point (`getProbeFade > 0`, which is upstream's own statement that a probe may
answer for a point at all). Both of the quantities that LOOK like coverage were
built and measured first, and neither is one:

  * `cubemapAccumWeight` is the sum of the INTER-PROBE BLEND weight, a quartic in
    the distance from a probe's GRID CELL centre that is divided out again ten
    lines further down; its magnitude carries no information. At the centre of a
    four-probe room it measures 0.006, so a "1 - that" rule floods a closed room
    with sky. Measured: gi.probe_open case 13's mirror went green.
  * the raw `getProbeFade` — the normalised depth inside the probe's own box —
    cannot be coverage either, because the box is SHRINK-FITTED TO THE ROOM'S
    SURFACES: every wall and floor pixel lies essentially ON a face of it and
    reads ~0.001 (measured, same case). Depth inside a box is not coverage when
    the box's faces are the things being shaded.

Binary is also what bounds the risk: a pixel any probe box contains renders
exactly what it rendered before, bit for bit, so no room, no probe suite and no
selftest pixel can move.

AND IT IS A SWAP, NOT A REPLACEMENT (second read). A pixel no probe box covers
may still be INSIDE the voxel volume, and there the cone has a real answer — the
crate beside this glossy floor, the overhang above it. What the cone does not
have is the sky: where its march leaves the volume without hitting anything it
fills that share with the scene's FLAT ambient, a 0-band copy of the sky. So
`Vct_piece_ps.any` exports, per cascade and at the same blendWeight, exactly what
that escape contributed and at what fraction, and the composite is

    envColourS = cone - ambient x escapeColour + sky x escapeFraction

with the cone's own hit colour untouched. The colour removed carries upstream's
1/pi (it is exactly what was added); the weight the sky enters at does NOT,
because every other cubemap sample in this shader is in radiance units — the two
conventions differ by pi in this pin, which upstream's own comment beside the
0.31831 records as unexplained. At a full escape the pixel therefore sees exactly
the sky it would see with no voxel volume at all, which is the calibrated answer
(`scripting.e2e.default_ground`'s 5/255, measured back to 5). Where the cone
never ran at all the whole lobe is the sky's.

Only the SPECULAR lobe, and the clear coat's. The diffuse half of the
environment belongs to the irradiance field and the cones, which lose nothing
when a probe is dropped; `cubemaps_as_diffuse_gi` stays masked off on our side.

INERT FOR UPSTREAM: every line is inside `@property( jah_sky_env_probe )`, a
property only Jahshaka's listener sets, and only for a colour pass of a scene
whose probe grid has taken the env slot. Without that property the generated
shader is byte-identical to the unpatched one.

KNOWN LIMIT, recorded rather than hidden: the coverage test is binary, so the sky
arrives at once when a glossy surface leaves the outermost probe box. Measured at
10/255 on a reflective ground in a new project with a crate (the band is outside
the building in every enclosed sample, so nothing crosses it there). No
shader-side ramp can fix it while the boxes are fitted to the very surfaces being
shaded — the answer is a margin on the FIT, and it belongs to a later lane.

Verified: `git apply --check` against the pin with patches 0001-0047 applied and
`git apply --reverse --check` after applying; `build-ogre.sh`'s stack re-applied
in the lane worktree; the `--engine-selftest` hash UNCHANGED at
b55e2d5d03dce4059b71a65f8e61dc4773fb0b048e3de05428aca83cabae0ec7 (the default
scene's matte ground has no probe grid and, behind patch 0028, no env term at
all); `gi.probe_open` green including its new case 4c, which asserts both halves
in pixels — a mirror inside the boxes reads blue (a probe's photograph of a blue
sky) and one 25 m outside them reads the green IBL cube exactly.

--- a/Samples/Media/Hlms/Pbs/Any/ForwardPlus_DecalsCubemaps_piece_ps.any
+++ b/Samples/Media/Hlms/Pbs/Any/ForwardPlus_DecalsCubemaps_piece_ps.any
@@ -157,6 +157,13 @@
 
 	midf cubemapAccumWeight = _h( 0 );
 
+	// Jahshaka patch 0048: HOW MANY PROBE BOXES CONTAIN THIS POINT. Not a
+	// weight -- a count, and the only thing read off it is whether it is zero.
+	// See the composite at the bottom of this piece.
+	@property( jah_sky_env_probe )
+		midf jahProbesHere = _h( 0 );
+	@end
+
 	midf3 pccEnvS = midf3_c( 0, 0, 0 );
 	midf3 pccEnvD = midf3_c( 0, 0, 0 );
 
@@ -195,6 +202,12 @@
 
 		if( probeFade > _h( 0 ) )
 		{
+			// Jahshaka patch 0048: this probe's box contains the shaded point.
+			// The test is the loop's own -- `getProbeFade > 0` is upstream's
+			// statement that a probe may answer for a point at all.
+			@property( jah_sky_env_probe )
+				jahProbesHere += _h( 1.0 );
+			@end
 			float2 cubemapIdx_priority = unpackUshort2ToFloat2( floatBitsToUint( probe.halfSize.w ) );
 			float probeCubemapIdx   = cubemapIdx_priority.x;
 			float probePriority     = cubemapIdx_priority.y;
@@ -467,6 +480,112 @@
 	@end
 
 
+	// ---- JAHSHAKA PATCH 0048: WHERE NO PROBE IS, THE SKY IS ---------------
+	//
+	// The environment slot of this pin's PBS shader has ONE occupant, and under
+	// automatic parallax-corrected cubemaps it is the probe cube ARRAY: the sky
+	// cubemap comes off every datablock the moment one probe exists, because a
+	// manual cube in that slot generates a shader that cannot compile (three
+	// ways -- OgreSky.cpp's note on reflectionTexFor has the list). That was
+	// survivable while a probe grid was an all-or-nothing scene-wide decision.
+	// It stopped being survivable when the grid became a PER PROBE decision
+	// (lane R5-ROOM): a PARTIAL grid is the normal case now -- one crate in an
+	// open scene keeps 7 of 18 candidates -- and every pixel that no surviving
+	// probe's box contains had no environment left but the cones, whose own
+	// escape term is a flat, pi-dimmed copy of the same sky.
+	//
+	// So the sky gets a second slot of its own, at the pass level, through the
+	// listener route HlmsPbs honours for exactly this purpose
+	// (getNumExtraPassTextures / propertiesMergedPreGenerationStep /
+	// hlmsTypeChanged; upstream's Terra sample is the reference use), and here
+	// it answers for the pixels the probe grid does not.
+	//
+	// THE TEST IS THE LOOP'S OWN, AND IT IS BINARY: did any probe's box contain
+	// this point. Upstream has no other statement of "a probe may answer here",
+	// and the two quantities that look like coverage are both measured NOT to
+	// be (this was built all three ways before it was written this way):
+	//
+	//   * cubemapAccumWeight is the sum of the INTER-PROBE BLEND weight, a
+	//     quartic in the distance from a probe's GRID CELL centre which is
+	//     divided out again ten lines above -- its magnitude carries no
+	//     information. At the centre of a four-probe room it is 0.006, so a
+	//     "1 - that" rule floods a closed room with sky (measured: gi.probe_open
+	//     case 13's mirror went green).
+	//   * the raw getProbeFade is the normalised depth inside the probe's own
+	//     box -- and the box is SHRINK-FITTED TO THE ROOM'S SURFACES, so every
+	//     wall and floor pixel lies essentially ON a face of it and reads ~0.001
+	//     (measured, same case). Depth inside the box cannot be coverage when
+	//     the box's faces are the things being shaded.
+	//
+	// Binary is also what keeps this patch honest about risk: a pixel any probe
+	// covers renders EXACTLY what it rendered before, bit for bit, so no room,
+	// no probe suite and no selftest pixel can move. Only the pixels that had
+	// no probe answer at all change, and they change from the cone's flat sky
+	// to the real one.
+	//
+	// The gain and the mip count come from our own pass float4 rather than from
+	// ambientUpperHemi.w and envMapNumMipmaps: both of those are shared with the
+	// probe array, whose captures must not be scaled by the Sky Light dial and
+	// whose mip chain is its own.
+	//
+	// ONLY THE SPECULAR LOBE. The diffuse half of the environment belongs to the
+	// irradiance field and the cones, which do not lose anything when a probe is
+	// dropped; cubemaps_as_diffuse_gi stays masked off on our side.
+	@property( jah_sky_env_probe )
+		if( jahProbesHere == _h( 0.0 ) )
+		{
+			midf3 jahSkyDirLS = mul( pixelData.reflDir, midf3x3_c( passBuf.invViewMatCubemap ) );
+			midf jahSkyLod = pixelData.perceptualRoughness * midf_c( passBuf.jahSky.y ) *
+							 ( _h( 2.0 ) - pixelData.perceptualRoughness );
+			midf3 jahSkyEnvS = OGRE_SampleLevelF16( jahSkyEnvProbe, jahSkyEnvSampler,
+													float3( jahSkyDirLS ),
+													float( jahSkyLod ) ).xyz *
+							   midf_c( passBuf.jahSky.x );
+			// THE SKY REPLACES THE CONE'S ESCAPE, NOT THE CONE.
+			//
+			// A pixel no probe box covers may still be INSIDE the voxel volume,
+			// and there the cone has a real answer: the crate standing beside
+			// this glossy floor, the overhang above it. What the cone does NOT
+			// have is the sky -- where its march left the volume without hitting
+			// anything it fills that share with the scene's FLAT ambient, a
+			// 0-band copy of the sky (Vct_piece_ps.any's specAlpha term). So the
+			// composite is a SWAP of exactly that term, at exactly its weight:
+			//
+			//     envColourS = cone - ambient x w + sky x w
+			//
+			// with both quantities exported from the cone itself rather than
+			// reconstructed here. Outside the voxel volume the cone did not run
+			// at all (vctSpecular.w is 0 and upstream's own ambient pieces are
+			// gated on that), so the whole lobe is unanswered and the weight is
+			// 1: the sky alone, which is the open-world case this patch is
+			// named for.
+			@property( vct_num_probes )
+				// `jahConeWeight` is 0 when the specular cone never ran for this
+				// pixel at all -- there is then nothing to keep and nothing to
+				// remove, and the whole lobe is the sky's.
+				midf jahSkyW = jahConeWeight > 0.0f ? saturate( midf_c( jahEscapeWeight ) )
+													: _h( 1.0 );
+				pixelData.envColourS.xyz = pixelData.envColourS.xyz -
+										   midf3_c( jahEscapeColour ) +
+										   jahSkyEnvS * jahSkyW;
+			@else
+				pixelData.envColourS.xyz = jahSkyEnvS;
+			@end
+			@property( clear_coat )
+				// The coat lobe has no cone term to keep -- the voxel arm writes
+				// envColourS only -- so it takes the sky at full strength.
+				midf jahSkyCoatLod = pixelData.clearCoatPerceptualRoughness *
+									 midf_c( passBuf.jahSky.y ) *
+									 ( _h( 2.0 ) - pixelData.clearCoatPerceptualRoughness );
+				pixelData.clearCoatEnvColourS = OGRE_SampleLevelF16( jahSkyEnvProbe,
+																	 jahSkyEnvSampler,
+																	 float3( jahSkyDirLS ),
+																	 float( jahSkyCoatLod ) ).xyz *
+												midf_c( passBuf.jahSky.x );
+			@end
+		}
+	@end
+
 	@property( vct_num_probes )
 	}
 	@end
--- a/Samples/Media/Hlms/Pbs/Any/Vct_piece_ps.any
+++ b/Samples/Media/Hlms/Pbs/Any/Vct_piece_ps.any
@@ -366,6 +366,12 @@
 	INLINE void computeVctProbe
 	(
 		OGRE_INOUT_REF( float3, outDiffuse ), OGRE_INOUT_REF( float4, outSpecular ),
+	@property( jah_sky_env_probe )
+		// Jahshaka patch 0048: what the SPECULAR cone escape contributed, so the
+		// piece downstream can swap it for the real sky. See the accumulation.
+		OGRE_INOUT_REF( float3, jahEscapeColour ), OGRE_INOUT_REF( float, jahEscapeWeight ),
+		OGRE_INOUT_REF( float, jahConeWeight ),
+	@end
 		PixelData pixelData, float3 pos, float3x3 TBN,
 	@property( vct_ambient_hemisphere )
 		float ambientWD, float ambientWS,
@@ -558,17 +564,50 @@
 			float specAlpha = ( 1.0f - min( 1.0f, irrLight.alpha / 0.95f ) );
 
 			@property( vct_ambient_hemisphere )
-				irrLight.colour.xyz += lerp( probeParams.ambientLowerHemi.xyz,
-											 probeParams.ambientUpperHemi.xyz, ambientWS ) *
-									   specAlpha;
+				float3 jahAmbientTerm = lerp( probeParams.ambientLowerHemi.xyz,
+											  probeParams.ambientUpperHemi.xyz, ambientWS );
 			@else
-				irrLight.colour.xyz += probeParams.ambientUpperHemi.xyz * specAlpha;
+				float3 jahAmbientTerm = probeParams.ambientUpperHemi.xyz;
 			@end
+			irrLight.colour.xyz += jahAmbientTerm * specAlpha;
 
 			// 0.31831 = 1 / PI, but I'm not sure why is it even needed. It's clearly
 			// overbrightened when it's not there
 			outSpecular += float4( irrLight.colour.xyz * blendWeight * 0.31831f, blend );
 
+			@property( jah_sky_env_probe )
+				// JAHSHAKA PATCH 0048: WHAT THE CONE COULD NOT ANSWER FOR.
+				//
+				// `specAlpha` is the share of this cone that left the voxel
+				// volume without hitting anything, and the line above fills that
+				// share with the scene's FLAT ambient -- a 0-band copy of the
+				// sky, divided by pi with the rest of the cone's result. The
+				// piece downstream replaces exactly that term with the sky
+				// cubemap in the direction the surface reflects, for pixels no
+				// probe box covers. To replace it, it must know precisely what
+				// was added and at what weight, per cascade and with the same
+				// blendWeight and 1/pi that scaled it -- so both are exported
+				// rather than reconstructed, and the cone's own HIT colour is
+				// never touched.
+				// THE COLOUR REMOVED CARRIES THE 1/PI; THE WEIGHT ADDED DOES NOT,
+				// and that asymmetry is the whole of the units question. What
+				// was ADDED here is the flat ambient scaled by the cone's own
+				// composite, 1/pi included, so removing it exactly means
+				// removing exactly that. What is ADDED BACK downstream is a
+				// cubemap sample, and every other cubemap sample in this shader
+				// (CubemapGlobal, the probe loop) is taken in radiance units
+				// with no 1/pi at all -- the two conventions differ by pi in
+				// this pin, which upstream's own comment three lines above
+				// records as unexplained. So the sky's weight is the ESCAPE
+				// FRACTION itself: at a full escape the pixel sees exactly the
+				// sky it would see with no voxel volume at all, which is the
+				// calibrated answer (scripting.e2e.default_ground's 5/255
+				// grazing margin, measured back to 5 with this).
+				jahEscapeColour += jahAmbientTerm * specAlpha * blendWeight * 0.31831f;
+				jahEscapeWeight += specAlpha * blendWeight;
+				jahConeWeight   += blendWeight;
+			@end
+
 			@property( use_parallax_correct_cubemaps )
 				vctSpecPosVS = fromVctToViewSpace( irrLight.posLS.xyz, probeParams );
 			@end
@@ -629,7 +668,18 @@
 
 	float3 vctDiffuse = float3( 0, 0, 0 );
 	float4 vctSpecular = float4( 0, 0, 0, 0 );
-	computeVctProbe( vctDiffuse, vctSpecular, pixelData, inPs.pos, TBN,
+	@property( jah_sky_env_probe )
+		// Jahshaka patch 0048: the cone's escape, read by the probe piece which
+		// runs later in this same scope.
+		float3 jahEscapeColour = float3( 0, 0, 0 );
+		float jahEscapeWeight = 0.0f;
+		float jahConeWeight = 0.0f;
+	@end
+	computeVctProbe( vctDiffuse, vctSpecular,
+	@property( jah_sky_env_probe )
+					 jahEscapeColour, jahEscapeWeight, jahConeWeight,
+	@end
+					 pixelData, inPs.pos, TBN,
 				@property( vct_ambient_hemisphere )
 					 ambientWD, ambientWS,
 				@end
